# Playground — experiments for future games

This directory holds standalone **experiments** (reusable libraries + brainstorm dumps, top-level folders) and **prototypes** (throwaway games that test ideas before branching into their own project, under `prototypes/`). A shared **library** of character/item/party blueprints (`library/`) is readable by every page on this origin. Future games built with Claude Code will read this directory, so every experiment must have a thorough `README.md` explaining its mechanisms, data formats, and how to reuse the code.

Owner: Radley Sustaire (independent project). Sandbox: auto-commit freely, no remote yet.

## Queueing work - Do Not Stop For Feedack

When work has been laid out and is ready for development, whether through rounds or milestones or feedback, it is absolutely critical that you complete all the work requested by the user. Do not stop to request feedback, unless specifically requested BY the user. Do not stop after a milestone has been reached if there are other milestones still pending. Use multiple agents, the `/loop` or `/goal` features, or other scheduled tasks to continue working until all pending work has been completed. If any tasks are put on hold or shelved, they should be noted in documentation and never forgotten, and brought up when all other work has been completed. It is acceptable to break projects into smaller batches, but what is not acceptable is pausing while work is still remaining.

A specific example of "what went wrong" was on 9/17/2026, the agent stopped after round 6, in the middle of the night, wasting 12 hours of productivity for the user to review work while the user was not even available to review it. There were 3 other rounds of updates pending that were simply delayed. This should be avoided at all costs. Delaying work is completely undesired.  The user returned to an incomplete project, wasting hours of additional time.

## Layout
- `<experiment>/` — a library other code imports (voice-lab, lingo, avatar-2d, avatar-3d, namegen, items, combined). Stable APIs, READMEs, tests.
- `prototypes/<name>/` — a small game built from the experiments. Throwaway: it may hard-code things and break when experiments change; when one graduates, copy it out to its own repo. Each has `index.html`, `README.md` (what it tests, what worked, what didn't), `js/`, `data/`, `tests/`.
- `library/` — blueprints (characters, npcs, items, parties, enemies) shared across pages via localStorage + defaults JSON; "Sync to Claude" posts them to `tools/serve.py`, which writes `library/synced/library.json` (git-ignored) that Claude reads with the Read tool.

## Conventions (follow these when adding an experiment or prototype)

1. **One folder per experiment**, kebab-case name, with `index.html` as the entry point and a `README.md` for Claude + humans.
2. **No build step.** Plain HTML, standalone `.css` files, ES modules (`type="module"`). Third-party libs are vendored under `vendor/` (see `vendor/README.md` for licenses).
3. **Shared helpers** live in `shared/`: `style.css` (dark minimal UI), `ui.js` (knobs, selects, panels, JSON export/import, seeded rng), `store.js` (namespaced localStorage), `tooltip.js` + `tooltip.css` (the shared tooltip engine: `installTooltips()` once, then `data-tip="text"`, `data-tip-html` or `data-tip-render` on any element — 150ms delay, flips at the screen edge, keyboard-focus friendly, no library), `langdebug.js` (language debug mode: click words in spoken lines to see/override pronunciations, export/import/submit), `voices.js` (party voice system: a timbre per class/role on our formant engine, varied by seed — `voiceFor({ role, gender, seed })`), `format.js` (one number formatter for every screen: `fmt` at most two decimals, `hp` whole, plus `pct`/`sign`/`range`/`secs` — nothing rendered should ever read "25.02000000000001"), `character-schema.md` + `character.example.json` (the shared character JSON that experiments can read/write). Sound is not in `shared/` but works the same way: `sfx/js/sfx.js` (`Sfx.create({ method })`, `play(id, { pan })`) is the one place any page gets sound effects from, and `prototypes/emberveil/js/sfx-bridge.js` shows how to hook a game without editing it.
4. **Serve** with `./serve.sh --bg` (runs `tools/serve.py`: static files + the `/api/library/sync` and `/api/inbox/<name>` JSON endpoints) → `http://<LAN-IP>:8400/` (LAN IP via `hostname -I | awk '{print $1}'`). The user cannot open localhost links; always give the LAN URL.
5. **Tests**: Playwright specs in `tests/` (root) or `<experiment>/tests/`, run with `npm test`. Node unit tests (`node --test`) for pure logic.
6. **When adding an experiment**: create the folder, add a card to `index.html`, add a section to the table below, add a line to `~/claude/docs/playground.md`, and write the README. Keep this file's table current.
7. **Data first**: anything a game would reuse (presets, phrase libraries, part catalogs) goes in JSON or a plain data module, separate from UI code.
8. Visual polish is not a goal. Wide option coverage is.
9. **No third-party IP in player-facing text or data.** Other games (Diablo, WoW/Skada, Mii, Tomodachi Life, RimWorld…) may be named only in chat and in these Claude-facing docs. Invent original names for enemies, places, items and mechanics. Exception: the voice engines named in the voice lab UI (espeak, Piper) are fine.

## Experiments

| Folder | What it is | Status | Key files |
|---|---|---|---|
| `voice-lab/` | Synthetic character voices: 5 engines (**our own formant synthesizer** `js/engines/formant/`, packaged as a versioned module: `js/formant-voice.js` + `engines/formant/module.json` + CHANGELOG, v1.1.0 with CMUdict + letter-to-sound rules, see `voice-lab/FORMANT.md`; espeak via meSpeak; our babble gibberish synth; Piper neural; Web Speech; SAM removed 2026-09-10) with license badges + pros/cons, Tomodachi-style generic knobs (pitch/speed/depth/tone/breath/rough/flutter/intonation/wordgap), pure-JS effects chain, 31 presets, engine compare, crowd stress test, phoneme input, voice JSON | done 2026-09-09 | `voice-lab/README.md`, `voice-lab/FORMANT.md`, `js/voice.js` (API), `js/engines/*.js`, `js/engines/formant/{g2p,rules,phonemes,tracks,synth,index}.js`, `js/fx.js`, `js/dsp.js`, `data/presets.json`, `data/cmudict/` |
| `lingo/` | Text language system: `{placeholder.modifier}` templates with plural/select/random blocks, lexicon with explicit forms (sg/pl/adj/people/lang), pronoun sets, articles, verb agreement, pronunciation (respell → espeak phonemes), tag-scored grammar (~1080 phrases / 80 intents, dark tone; combat pools tagged by enemy family + hero role, session anti-repeat via `meta.noRepeat` so nothing repeats inside ~20 fights), 25 RimWorld-style traits, sliders, Tomodachi custom slots, 14 verbal tics, anti-repeat, conversation planner, dictionary editor; **memory system** (trait-weighted importance, half-life decay, merging, relevance-based recall, 16 event types), **scene awareness** (place/tags/threats/danger → observe/fear/plan lines, planner bias) and **relationships** (multi-factor feelings + knowledge, trait-scaled events, memory hook, tone tags) | done 2026-09-10 | `lingo/README.md`, `lingo/MEMORY.md`, `lingo/RELATIONS.md`, `js/lingo.js`, `js/memory.js`, `js/context.js`, `js/relations.js`, `data/{lexicon,grammar,traits,speakers,events,scenes,relations}.json` |
| `avatar-2d/` | SVG paper-doll builder, chibi, 148 + ~85 gear parts (`js/parts/gear.js`: helmets, armour, capes, held weapons, off-hand items) across 17 slots, Mii-style face sliders, body height/width/headSize via group transforms, CSS-variable recolour, seeded random with race rules (7 races), 15 presets, gallery, PNG/SVG/JSON export | done 2026-09-09 | `avatar-2d/README.md`, `js/render.js`, `js/parts/*.js`, `js/random.js`, `data/presets.json` |
| `avatar-3d/` | Three.js builder reading the same JSON: Mii-style procedural body with the 2D face rasterized onto a face patch + procedural hair/hats/clothes + idle/walk/run/wave/talk/dead; Quaternius CC0 mode (2 bodies, 6 hairstyles, 2 outfit sets, 43 animation clips) with bone-scaled proportions; **37 creatures** (`creatures.html`) over six body plans — quad (wolf, dire wolf, boar, bear, rat, horse, deer, hound, cat, frog, drake, dragon, hyena, saber cat, crocodile, turtle, griffin), spider (spider, beetle), bat (bat, owl, moth, phoenix), snake (snake, worm, centipede), biped (golem, titan, imp), float (elemental, wisp, shard, wraith, horror, slime, mushroom, mimic); ten ready-made designs on the newer types in `data/creature-variants.json`, not wired into any game — with idle/walk/run/attack/talk/dead/fly and the `creature` JSON section; the type catalog lives in `js/creature-types.js` (no Three.js, so tools and node tests read it); **spell effects** (`js/spellfx.js`, demo `spellfx.html`) — 11 elements of projectile + impact built from shaped geometry (flame cones, spinning shard clusters, expanding torus rings, ground rune discs, jagged bolt lines, arrows) and the fx sprite sheet, 23 looping status auras parented to a body and scaled to its height, plus cast/heal/revive/aoe; no glowing spheres, additive blending, global `scale` (stages use 1.4 for the further-back fight camera), wired into both prototypes' stages; **vehicles** (`vehicles.html`): 7 procedural travel vehicles over 5 plans (hand cart, pack mule, covered wagon, ox cart, plated war wagon, closed coach, drake-drawn sled) with real creature bodies in the shafts, built facing +x, `metrics()` giving hitch/seat points | done 2026-09-13 | `avatar-3d/README.md`, `js/mii.js`, `js/creatures.js`, `js/creature-types.js`, `js/quaternius.js`, `js/vehicles.js`, `js/face-texture.js`, `js/scene.js`, `js/spellfx.js`, `assets/quaternius/` |
| `namegen/` | Name Forge: per-race languages (phonology + concept dictionary, 12 races, 268 tagged concepts), pattern grammar for people/factions/regions/settlements/landmarks/artifacts/mottos, glosses, forms (member/members/adj/people/short/possessive), respell pronunciation, Lingo lexicon export | done 2026-09-10 | `namegen/README.md`, `js/namegen.js`, `data/{languages,concepts,patterns}.json` |
| `items/` | Item Vault: ~340-item tagged catalog with race affinity weights + exclusives, materials/qualities/enchants, loot roller with Name Forge artifact names and lore, Lingo export | done 2026-09-10 | `items/README.md`, `js/items.js`, `data/{items,materials}.json`, `tools/build-items.py` |
| `library/` | Blueprint library: characters/npcs/items/parties/enemies as JSON (kind `enemy` = the Emberveil bestiary, `npc` also holds its pets and companions), defaults seeded from the demos, localStorage for user entries, export/import, sync to Claude via the dev server | done 2026-09-10 | `library/README.md`, `js/library.js`, `data/defaults.json`, `tools/serve.py` |
| `conversations/` | Structured conversations on top of Lingo + memory (82 topics incl. callbacks, nemeses, companions, quests, revive thanks, 12 rare rewarding ones; 6 multi-day threads; variant generator): topics with asker/answerer/third roles, requirements (memories with `maxAgeHours`/`minAgeHours`/`bindingIs` — the last casts the person named in a memory, e.g. the healer who revived you — gear with kill counts from the meter, better/worse than the replaced item, bag items ignored, party facts, traits, relations), variant lines with conditions, spoken through Lingo so personality applies; `factsFrom()` adapts a game's data | done 2026-09-11 | `conversations/README.md`, `js/conversations.js`, `data/topics.json` |
| `sfx/` | Sound Lab: sound effects over a catalog of 118 logical ids (11 elements × launch/travel/impact, 23 statuses × apply/tick, melee, deaths, loot by rarity, stings, interface, 8 ambience loops) with 4 interchangeable methods behind a dropdown + licence badges — `synth` (our layered Web Audio recipes, seeded variation), `library` (Kenney CC0 packs vendored in `assets/kenney/`), `hybrid` (default), `retro` (chiptune); **loudness normalization**: K-weighted measurement, per-category targets owned by `js/loudness.js` (`CATEGORY_TARGETS`; ambience 10 dB under everything else, loops only ever cut, ambience bus capped), soft ceiling at −1 dBFS, per-id trim, sfx/ui/ambience buses + master limiter; gallery with level meters, A/B compare across methods and a before/after loudness table | done 2026-09-13 | `sfx/README.md`, `js/sfx.js`, `js/loudness.js`, `js/methods/*.js`, `data/catalog.json`, `tools/build-catalog.py`, `assets/kenney/README.md` |
| `meters/` | Skada-style damage meter: record every hit/heal/miss/absorb/status/death (source, target, via, type, crit, overkill, blocked/mitigated/absorbed, overheal, killing blow, item), drill down actor → source → hit with per-target totals and sparklines, damage/healing/taken/absorbs/statuses/deaths modes, per-fight or whole run, per-item kill counts; drop-in UI component | done 2026-09-11 | `meters/README.md`, `js/meter.js`, `js/meter-ui.js` |
| `worldgen/` | World Forge: procedural world maps, macro→micro, three zoom levels. **Weather** (`js/weather.js`, 2026-09-15): 14 states chosen from a cell's climate (no snow in a desert, no rain on a dry world, storms need heat and water, volcanic ground throws ash, raw magic throws ion storms), a `WeatherClock` that crossfades between them, a `weather` map layer + legend + hover readout + per-world profile in the viewer, and `atmospherePalette()` which varies a planet's sky/sea/cloud colour by its own seed as far as that world's `extremity` allows. 7 continent methods (noise, plates, voronoi, diamond, archipelago, pangea, mixed), thermal + droplet erosion, percentile sea level, wind-swept rainfall with rain shadow, depression-filled drainage → rivers/lakes, 25-biome table with evil/good/magic aura biomes, cheapest-path named regions, named seas/ranges/rivers/lakes/forests, settlements by habitability + landmarks/dungeons/ports/passes, A* road network with bridges and sea lanes, region detail (6×) and 64×64 local tiles, canvas renderers + debug layers, JSON/PNG export, small history table; viewer with ~30 knobs, 5 presets and a Web Worker | done 2026-09-12 | `worldgen/README.md`, `js/{noise,biomes,names,world,regions,nodes,roads,local,render,export,history,worker,app}.js` |
| `assets/` | Shared art library: scenery backdrops (32 standalone `viewBox="0 0 100 100"` SVG scenes, day + already-dark ones, tagged by place/biome/act), 13 map node icons and **35 fx particle sprites** (`data/fx/*.svg`, 64×64 transparent: flames, shards, runes, claws, slashes, bolts and status glyphs) that `avatar-3d/js/spellfx.js` rasterizes to textures, indexed by `data/manifest.json`; loader with tag lookup, night wash, caching, `fxTexture()`/`fxTextures()` (Three.js passed in, so the loader stays 3D-free) and a never-throws fallback; gallery page. Used by both prototypes' stages | done 2026-09-12 | `assets/README.md`, `js/assets.js`, `data/manifest.json`, `data/scenery/*.svg`, `data/icons/*.svg`, `data/fx/*.svg` |
| `universe/` | **Star Forge**: a seeded universe — `galaxy.js` (5 layouts: spiral arms/elliptical/cluster/ring/scattered, class-mix sliders, connected travel lanes), `stars.js` (11 classes incl. red dwarf, red giant, white dwarf, neutron star, binary pair, black hole with accretion disc; habitable zone + frost line from brightness), `system.js` (14 planet archetypes incl. the rare living world 3–8%, gas/ice giants, tidally locked, crystal, void-touched; rings, belts, comets; baseline resources + 1–2 rare elements per planet from `data/elements.json`; **orbit spacing** — a ratio ladder with a floor of 1.5× inside the frost line and 1.7× past it, plus `orbitLayout()`, the drawing ladder that keeps a minimum gap between rings and caps a planet's drawn size at a third of its gap), **moons as small planets** (4 kinds: barren, ice, volcanic from tidal heat, and the rare living moon of a giant in the water zone — each with a stable id `<planetId>m<i>`, its own seed, resources scaled by size, its own half-size surface map, selectable in the viewer and passed through Frontier Foundry's `universePlanets` adapter as a landing target), `planetmap.js` (planet *or moon* → World Forge knobs → a real surface map, single-biome bodies locked to one family, `moonMapSize`), `texture.js` (equirect surface/cloud/night-lights/glow canvases). Viewer with galaxy → system → planet → moon → map views. 3D models live in `assets/js/space-models.js` | done 2026-09-14 |
| `proctown/` | **Procedural Towns**: THE town planner, imported by `prototypes/farhold/` so there is no second copy to drift. A town is cut into blocks by recursive binary subdivision and **the cuts become the streets**, so a building can never sit on a road (`overlaps()` is a hard assertion, and a test checks 7 cultures x 11 seeds x 6 sizes). Each plot knows which edge fronts a street, so a door is never on a blank back wall. Street hierarchy falls out of the recursion - the first cuts run the whole town and are `main`, the deep ones are `alley`. Seven cultures (human, elf, dwarf, undead, orc, halfling, desert) are PARAMETER SETS, not code: `jitter` alone takes a dwarf from a rigid grid (0.04) to a halfling's wander (0.48). Tuning page with seed stepping, culture/size knobs, plot/block/frontage overlays, four-seed compare (the sameness test you can do with your eyes), a highlight search, a live overlap verdict and a 200-town spread report. **The building kit** (`proctown/kit.html`, `proctown/js/buildkit.js` + `drawkit.js`): every part a building is made of — walls, roofs, doors, windows, chimneys, stalls — drawn on its own, so a roof that does not meet its walls is obvious here rather than three hundred metres away in the game | done 2026-09-19 |
| `combined/` | Character sheet: two full characters (avatar+voice+speech) in one 3D scene, talking with generated lines, their own voices and talk animation; scene picker + memory roller/clock feeding the conversation; random full characters; JSON round-trip | done 2026-09-10 | `combined/README.md`, `js/app.js`, `data/characters.json` |

### Chibi 2 milestone

`avatar-3d/chibi2.html` is the first Chibi 2 comparison: three humanoid presets, an 18-bone rig,
two merged skinned meshes per character and 12 animations, with eight-fighter spell benchmarks.
`avatar-3d/CHIBI2.md` documents the API, performance budgets and incomplete part mappings.
`js/spellfx-batched.js` adds opt-in instanced sprite rendering without replacing `spellfx.js`.
Emberveil uses Chibi 2 humanoids and batched sprites by default; `?renderer=chibi1` selects the original presentation.

## Prototypes

| Folder | What it tests | Status |
|---|---|---|
| `prototypes/emberveil/` | **Emberveil 2**: rebuild of the user's Emberveil RPG (`~/claude/emberveil`, read-only source): data copied by `tools/build-emberveil-data.mjs` (+ `expand-emberveil-map.mjs`, zones ~50% longer), 30 classes as Mii characters, loot with affixes/uniques/sets (`js/loot.js`), stats/talents/passives (`js/rules.js`), auto-battle simulator with meter records (`js/combat.js`), world map + events + towns + travel days/supplies/vehicles/night attacks/rest (`js/game.js`), memories + relations + our formant voices, camp conversations with callbacks (conversations/), damage meter tab (meters/), named enemies + nemeses (`data/named-enemies.json`, original names only), side quests, hero errands (`data/class-quests.json`), conversation threads + rare rewards, journal, companion memories, party voices (`shared/voices.js`), language debug mode, combat speech variety (enemy/boss/named/nemesis/beast/night-raid/ambush openers through Lingo, `js/talk.js`), map layout + icons, Lingo pack `lingo/data/packs/emberveil.json`, designed looks for the whole bestiary (`data/enemy-looks.json` from `tools/build-emberveil-enemies.mjs`: 80 enemy/boss/pet/companion/hire looks, 2D avatars for humanoids + creature specs for monsters, filed in the library as `enemy_<id>` / `companion_<id>`), **themed dark-fantasy UI** (`js/ui.js` + the `ui` art in `assets/data/ui/`: leather panels, gold frame flourishes, ember accents, Cinzel/Spectral, tooltips everywhere, in-game menu overlay), **effect registry** (`js/effects.js` + `EFFECTS.md`: all 359 effect ids from the original — 12 legendary powers, 72 affixes including every `cond_*`, 214 skill/talent/upgrade keys, 29 statuses, 10 champion + 9 named modifiers, 7 enemy-spell + 6 boss-phase keys — as data-driven hooks that `combat.js`/`rules.js`/`loot.js`/`game.js` dispatch; 176 of them were dead data in the original; one table-driven test per id in `tests/effects.test.js`), **the road weapons** (22 new bases + 12 new uniques in `data/items.json`, each with a property that touches a system this rebuild added — forage rations, night-attack chance, extra travel moves, exhaustion relief, standing a watch, growth from the meter's per-item kill counts, elemental brands with the matching stage effect, sunder, bonus vs champions/named/nemeses, companion buffs, vehicle damage, kill memories and relationship swings; carried as `intrinsic` affixes with `minAct` gating and a held-part `look`, run by new **world hooks** in the registry (`legs`/`nightChance`/`exhaustionEase`/`nemesisChance`/`onLeg`/`onRest`/`onWin`) dispatched from `game.js`; four camp topics require them; one row per weapon in `tests/weapons.test.js`), **balance simulator** `tools/sim-emberveil.mjs` (300 seeded headless runs with the real modules and a bot that spends level-up points, walks, rests, shops, plays crossings and re-equips → `research/sim-report-3.md` (round 20's difficulty pass; round 19 is `sim-report-2.md`, round 14 `sim-report.md`): per-act cleared/stalled/wiped funnel, difficulty curve (rounds and share of the party's health bar per fight, enemy HP/damage faced, gear score), level/XP/gold/gear at each act boundary against the XP table, loot and gold per fight with the rarity mix, crossing pass rates, damage share by source and by class, status uptime, what kills the party, starvation, worn affixes/uniques/legendaries, class + weapon matrices) and `data/balance.json` as the real knob file (`rules.applyBalance`: enemy global/per-act HP-damage-armour-magicResist, boss/champion/named multipliers, `economy` XP-gold-shop-drop, `progression.xpTable`, `partySize`, `combat.skill`; a `loot` block read by `loot.js`; a `world` block for the travel layer; `crossings.json`'s `difficulty` block read by `explore.js`; **round 20's difficulty pass** rebuilt `enemies.actMultipliers` for acts 2–6 with damage climbing faster than HP, stretched the top of `progression.xpTable` and trimmed gold, taking full six-act clears from 38.3% to 12.8% over five seeds with act 1 at 86–88% and no act acting as a wall); **crossings** (`data/crossings.json` + `js/explore.js`): a `crossing` node type, 1–2 per zone from the map tool, playing a travel scene across the stage then a hazard with checks/items/gold/fight answers, victory-shaped rewards, memories and a retry cost; the party's **vehicle** drawn on the stage (parked on the road, at the camp fire, rolling during a crossing) from `avatar-3d/js/vehicles.js`; aspect-aware stage framing (`Stage.frame`) and speech bubbles clamped inside the stage box; node labels that can no longer promise a head count the fight does not have (`countWordIn`/`labelFits`/`encounterLabel`); a **Narrator** for scene text (`js/talk.js` `isSceneText`/`narrate`, own voice from `shared/voices.js`, italic in the log), weapon-aware speech (`weaponType`/`ammo` bindings, no more "I'm out of bows!"), a **Named Foes** board in the Journal fed by the meter's killing blows, one wording + an animated d20 popup for every skill check (`js/ui.js` `checkText`/`skillCheckPopup`, `skillcheck.css`), and a binding audit that renders every pool, memory and topic and fails on a stray `{` (`tests/bindings.test.js`); **round 20**: the map is a layered road (`normalizeZones()` in the map tool — max 4 branches open, every trail one column long, no shortcut past the middle of an act, `tests/zone-graph.test.js` checks all 13), travel is one node at a time in either direction with no fast travel (so `addTowns()` gave every act zone a settlement), **revives are a real rule** (the fallen stay down; healer / Emberheart Draught / shrine / town cleric, `hero.revivedBy` + a `revive` memory at importance 0.95 that camp topics thank people for), night raids at ~35% and harder + better paid (`balance.json` `world.nightRaid`), quest pins on the map and strangers on the road offering better-paid jobs (`data/road-quests.json`), humanoid fights for toll/bandit/warden events (`data/enemy-families.json` — a toll no longer summons cinder hounds), a hero switcher on the Skills tab, an item card you pick the hero on first (deltas against *their* gear, weapon type + class restrictions up front), per-hero rename/voice settings, and the original's real-time weapon layer removed everywhere (`tests/world-rules.test.js`) | done 2026-09-14 |
| `prototypes/frontier-foundry/` | **Frontier Foundry**: 4X + RTS automation, playable end to end. Land on a worldgen local tile, scan hidden resource nodes, mine, haul, refine, build (outline -> builders walk over -> done), research, defend tower-defence waves, launch satellites/probes/a rocket, carry the hold to the next planet; beacons on 3 worlds + an orbital station wins. Storage **pools** instead of belts (stores that reach each other share everything; further away needs a truck), a power grid with duty-cycled generators and load shedding, ten planet archetypes with eight rare elements and ten planet-only buildings. Data: 77 resources, 101 structures, 67 recipes, 82 techs, 10 vehicles, 22 units, 25 quests, 43 notification templates. **Interface** (`index.html` + `js/ui/*`, an 'operations console' in cyan and amber): title/system survey, canvas surface map with fog + build ghosts + route tool + minimap, research tree, planet map, 3D orbit screen, quest board and codex, save/load, `window.foundry` for tests. **The map streams**: the grid is a 5x5 block of worldgen cells (480x480 tiles, centre 3x3 warm at landfall) in one flat typed-array set, chunks generated around the camera one a frame, biome seams dithered; `chunks` defaults to 1 so the node tests and the sim are unchanged. `patch()` in `js/ui/dom.js` is the keyed DOM morph any live-redrawing panel must use instead of `fill()`. `js/planets.js` carries a ten-archetype stand-in **and** an adapter for `universe/` (`universePlanets`). `tools/sim-foundry.mjs --why` is the balance harness; its bot stalls mid-game on its own base layout and does not reach a rocket yet | done 2026-09-14 |
| `prototypes/farhold/` | **Farhold**: a third-person RPG on a whole planet, and off it. Phases 1-10 + the round-3 queue are in `README.md`; **round 4 is the RPG expansion** and is documented in `prototypes/farhold/RPG.md`: level-banded **zones** from the World Forge region graph (`js/zones.js`, bands that tile 1-30 with no gaps, an overlay on the map and a banner when you cross a border), a bestiary of **46 enemies + 6 bosses + 10 companions + 14 champion/rare modifiers** (`data/enemies.json`, everything rescaled so nothing is knee-high), **packs, champions and rares** with visible auras and Name Forge names, **11 set-piece encounters** (`js/encounters.js`), **real dungeon interiors** (`js/dungeon.js` + the pure `js/dungeon-plan.js`), **chests, loot bags and the shared reward popup**, **crafting from recycled gear only** (`js/craft.js` + `data/crafting.json`: three materials, a Crafting tab that forges a base you pick and an Upgrade tab that tempers / promotes / inscribes / reweaves / recasts / brands), **every one of items.json's 63 affix stats and 24 legendary powers wired** (`js/effects.js` — nothing is inert, and a test fails if anything goes back), **39 skills over 6 slots unlocked by level**, **all 30 Emberveil classes** with **companions** for 13 of them (`js/pets.js`), **light you carry** (`js/light.js`), a **tabbed sheet** with rich item tooltips, **seamless take-off and landing** through the atmosphere (`js/atmos.js`) with reticles and an information card on whatever you point at in space, and **landable moons**. The round-4b play-test fixed NaN damage, night starts, off-centre aiming, terrain spikes, the galaxy drawing over the planets, walls built ninety degrees out, and two dozen others — all listed in `RPG.md`. **Round 5** closes the six items that round 4 flagged as not built: a **star chart on `M` whenever there is no ground under you** (`js/starchart.js` — system → nearby stars → arm → galaxy, every view centred on you) with **travel to nearby stars** over a real Star Forge disc (a star carries its own seed, so a star *is* a system) through a five-second **warp tunnel** (`js/warp.js`) that lands you in a system with its own skybox palette; **one marker book** (`js/markers.js`) holding quests, story objectives and dropped pins, each filed under the world it is on, drawn on the map with a Tracking list, on the minimap as a glyph or a rim arrow with the distance, and in space as glyphs over a world's bracket; a **continuous approach from orbit** (the throttle is governed by how close you are, the world you are closing on is rebuilt at two finer detail steps, warp locks out near anything, and below half a radius you fall into the atmosphere with no key press — then `view.setViewScale` stretches the clipmap to cover ten kilometres for the same 13,552 triangles); a **Habitable start** knob, on by default, that walks the seed until it finds a lived-in multi-biome world; **water that meets its bank** (`js/water-plan.js` — the river sheet is pushed out to where the bank comes back up, with a skirt for flat deltas, and lakes finally have water in them at one level); and `BESTIARY-IDEAS.md`, ~40 creatures written out Dwarf-Fortress-style and tagged by how much work each body is. **Rounds 6-9** are a 46-item play-test list, written up at the end of `RPG.md`. The headline is that **the seven reported affix bugs were one bug**: items.json stores some percentages as fractions and some as plain percentages, and Farhold's two readers picked opposite sides - so 0.02 crit chance printed "0%" AND did nothing, while 19.93 vs undead printed "+1993%". `js/affixes.js` states per stat which unit its consumer reads, restates the table once at load, then applies floors, caps and slot rules on top, with **item levels** and affix tiers over that. Also: **a weapon is a PATTERN** (`js/weapons.js` - a longsword is slash-slash-overhead at 3.0 m, a dagger jab-jab-slash at 1.9 m and twice the speed), which is the one idea that covers dual wielding, two-handers, elemental staves and the area stat; a **perk forest** (`js/perks.js`, 89 generated nodes) replacing attribute point-buy, passives and talents, where the shape of the tree is the cost; a **three-tier talent tree per skill** (`js/skilltalents.js`) that changes the plan cast rather than a stat; **twelve town buildings and a real street plan** (`js/town-plan.js`); **level 50** with planets banded low/medium/high; **meteors**; **loot beacons** whose colour is the rarity a chest is guaranteed to hold; shops with categories, buyback and a gambler; and a map that pans, zooms to the whole planet and draws World Forge's region detail instead of a bigger blur. Traps: `Effects.update()` ticked its timers and nothing rebuilt the derived sheet, so five timed affixes were silently dead; a DoT paying `perSecond * dt` sixty times a second reads as "1 damage" however correct the total; and **items.json is SHARED with Emberveil**, which has its own registry and a test that every affix in it resolves - anything only one game understands is injected at load, never written into the file.  **Round 10** is the play-test list plus two bigger pieces, all written up in `RPG.md` and `EXPANSION.md`: a level-1 character was being dropped into a **level 30-33 zone** because a world's difficulty band came partly from `seed % 3` (47% of seeds), so the band now comes from what the world IS and the low band is claimed first for whichever world a newcomer lands on — every system carries a body for every band (moons count) and the starting system always holds somewhere you can breathe; clicking the **perk tree** picked whatever was ~100px below the cursor (the canvas buffer was measured from its parent and one device-pixel ratio was used on both axes) and the forest is now a **lattice** with zoom and pan rather than a scatter; the **character sheet is a full screen** (`research/ui-round10-design.md` — header, tab rail with unspent-point badges, paper-doll equip figure, inventory filters + a standing compare panel, three-tier talent columns, nine-pane journal; nothing scrolls but a pane body) which also turned up three real bugs — the Perks screen was titled "Character", **every** skill was offered the bolt talent tree, and the XP bar was wrong above 30; **W and S fly forward again** (the longitude wrap was not bit-exact, so atmos.js's edge-brake fired ~45x a second mid-map: 80 m in 20s instead of 15.3 km) and the poles wrap; **planet size** gets Tiny and Super tiny, defaults to Small, and finally reaches the camps, dungeon doors, horizon and flight model that all read a hard-coded 640 m a cell; and **save/load** stopped dropping `world`, `quests` and `campaign` out of `snapshot()`'s own argument list, which is why a run saved in a town reloaded into open ocean and every load emptied the quest log. **The Territory** (`EXPANSION.md`) makes a zone a place with people in it: 12 factions whose standing moves for what you did (a deed for one is a third of a deed against their rivals, so nobody can be liked by everybody), a territory record a zone generated from the seed with only the deltas saved, patrols on real road nodes, caravans to trade with / escort / rob / find the wreck of, 14 kinds of person on the road, 12 things that happen to a zone, rumours that are a sentence and never a pin, and a job generator of 22 frames that will only offer one when every slot binds to something that exists right now — nothing is invented, so nothing can send you to an empty field. **The round-10 review** (`research/review-round10.md`, 45 findings, all implemented): the screens were fine and the **connective tissue** was missing - the game never said what to do, never explained what it had just built, and two content files (`data/landmarks.json`, `data/faction-rewards.json`) were parsed at boot and thrown away. Now: a tracked objective on the HUD, a class preview beside the 30-entry dropdown, the level-up message naming the perk point it actually gives (`pendingAttr` was never incremented anywhere - the perk forest replaced point-buy in round 7 and the message was never changed), the map naming only regions you have entered or heard about, **key rebinding** over 14 actions through a capture-phase listener (there was no binding table to build over, so it translates the event), the star chart's two measurement bugs (it measured its wrapper, and drew CSS pixels on a 2x buffer, which is why five worlds were 3px specks), a torch that is a flame not a spotlight (a Three.js point light with `distance` dumps its falloff over the last fifth - the cutoff is pushed out 2.2x and decay does the fading), the hooded mage's cowl trimmed inside his own shoulders (`avatar-3d/js/chibi2.js`, SHARED with Emberveil), and rumours signed by a real person out of `folk.roster()`. **Mounts, lights, boats and ships** (`js/gear.js`, `js/boat.js`): three of each, the dividing line being whether a thing is loot - a mount/lantern/quiver rolls a rarity and upgrades at the bench, a boat/ship is bought once and picked from a dropdown. Every shelf now carries all three mounts and all three lights (it used to guarantee the cheapest and roll for ONE of the rest), sorted into racks with headings, and a shop row carries its own spec + an upgrade/worse mark from `itemScore`. **Boats equip themselves** on entering deep water and the hull sets the speed (raft 3.4 / skiff 5.6 / cutter 8.2 against a 2.7 swim, a 5.4 walk and an 11.3 sprint) - `js/player.js` had never read the vehicle data at all. Two traps: the boat was put away by watching for the TRANSITION out of swimming, which `teleport()`, a load and a dungeon floor all skip, so the raft stayed equipped on dry land; and the shared `Loot.price()` (`prototypes/emberveil/js/loot.js`) prices everything off ONE global `basePrice` and ignores the price an item carries, so a 620g Wisp Lamp sat on the shelf at 30g - Farhold's `rpg.price()` honours `item.basePrice` and the shared file is left alone. **Rounds 11-12 and the building expansion** (`BUILD-MODE.md` §§0, 12-18, `RPG.md` "Rounds 11 and 12"): this round's theme was **finished modules that nothing called** - seven joins that had never been made, each indistinguishable from a missing feature and much harder to spot than a crash. Nothing called `build.confirm()` (clicking in build mode drew a sword); the ghost was aimed at the player's own FEET; nothing called `grid.add`/`stores.add` (a generator generated nothing, a crate held nothing, a 25 kW waypoint pad could never light); `stores` and `grid` were not in the save (a reloaded base was empty boxes beside a dead generator); ore was built with `createNodeField({data, seed, terrain})`, which takes NONE of those - `area` defaults to a 300 m circle at the ORIGIN, so every seam in the game was 29 km from where the player lands; nothing called `works.place`, so 16 machines and 61 recipes stood on the grass as geometry; `js/raid.js` was imported by nothing at all; `colony.setBase()` was never called, so appeal was 0, migration never fired and prosperity was permanently 1.00; and neither `js/vehicles.js` nor `avatar-3d/js/ground-vehicles.js` was imported. Now playable end to end: **`B` opens a real panel** (`js/build-ui.js` - four numbered first steps, tools with Level first, catalogue by group, live prices against what you are holding, and the live reason the ghost is red), the cursor is a ray marched from the camera, **E works a seam** (`js/mining.js` + `js/ore-view.js`), a **drill on a seam plus a route** delivers at `haulThroughput(metres)` so double the distance halves the delivery, a **bench you walk up to** takes a recipe, **turrets shoot** (all 19 defensive structures had no numbers at all; seven got a `defence` block and the gate is the BLOCK not the category so a palisade never snipes), a **raid you ring for** (`js/defence.js` - offer, take it on, ring the bell, and the world is quiet between them), a **Holding** with migrants, taxes and Necesse farming (your folk replant but never break new ground), a **Garage** (motorcycle/car/truck on `G`, Shift is the throttle), and **§9's sky**: no ship, no purchase, four subsystems at an assembler then a levelled pad - and `refuel()`, because `canLaunch` refused and `spendFlightFuel` spent while NOTHING ever put fuel in. `js/homes.js` is every base you ever raised, filed by BOTH systemSeed and planetId (every world uses the same metre grid, so matching on planetId alone folds you to the right coordinates on the wrong planet), with `routeTo` naming three legs - here / land / jump. Map pads now SELECT on click and a button travels; "Go here" moved to Settings -> Debug, on by default. Two bug classes worth keeping: **a settlement id is a number and the first one is 0**, so every `if (padPick)` dropped the pad nearest the middle of the map; and **three TDZ crashes** from `const`s read above their declaration, none of which `node --check` can see. **Closing the round** found five more of the same thing, making TWELVE finished modules with no way in and not one genuine algorithmic bug among them: js/shipyard.js had `stationProgress`/`stationGate`/`buildStationModule`/`nextStep` and NOTHING imported any of them, so §9.15's orbital yard and §9.16's "what to build next" were both unreachable; js/work.js did the whole ten-units work system with no screen at all; `colony.recruitOffer` and its refilling `townPool` had no way in (talking to any non-guard in a town now brings them home, with a BODY at the claim stone via `folk.spawnOne`); and `board` was declared TWICE on window.farhold so the later one silently won — a duplicate key in an object literal is not an error in JavaScript, it is a shrug. §1.4's **radial density falloff** closes the graphics round: a cell is thinned by how far out it is, and the rule that makes it safe is that a thinned cell is a SUBSET of the dense one — approaching a wood adds trees BETWEEN the ones you could already see. `tests/density.spec.js` caught a bug in my own fix on its first run: `break`ing out of the scatter loop consumed fewer rng calls, and the cell's rng is shared with the ruin roll, the megaflora and the grass, so the bushes jumped while the trees stood still | done 2026-09-20 |
| `prototypes/party-quest/` | Party of 4 (library/defaults/custom), race + class without stats, auto-battle with reactive dialog (beasts are creature bodies and only snarl), text-adventure travel with events and a minigame, town NPCs with disposition + deeds + quests + shop, day/night with campfire conversations from memories/relations, Mii 3D cinematic stage, save NPCs to the library | done 2026-09-10 |

## Tests
`npm test` (Playwright, serial: specs across all experiments incl. headless WebGL + audio synthesis) and `npm run test:unit` (node tests: lingo engine + data validation, avatar-2d renderer/random, sfx catalog + loudness maths). Screenshots land in `test-results/`.

## Building a game from this
Read **`GAME-GUIDE.md`**: what each piece gives you, import lines, data it needs, runtime cost, licenses, and recipes (talking NPC with memory + feelings, world generation, crowds).

## Shared character JSON

All experiments read and write sections of one character document (see `shared/character-schema.md`). Each section is independent: a game may use only `avatar`, only `voice`, or only `speech`.

```json
{ "schema": 1, "name": "…", "avatar": { … }, "voice": { … }, "speech": { … } }
```

## Research notes

`docs/research-*.md` hold the research done before building (voice engines, language systems, avatar assets) including licenses and rejected options. Read them before changing an engine or asset source.
